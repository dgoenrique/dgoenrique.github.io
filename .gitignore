# --- Hugo ---
# The folder generated with the final site (GitHub Actions will create this there)
public/
# Resource cache (resized images, processed CSS)
resources/_gen/
# Statistics files generated by Hugo (optional, but common)
hugo_stats.json

# --- Operating System (Linux/Mac/Windows) ---
# Mac system files (in case you open the project on a Mac one day)
.DS_Store
# Windows thumbnail files
Thumbs.db
# Linux/Editors temporary files (Vim, Nano, etc.)
*~
*.swp
*.swo

# --- IDEs and Editors ---
# VS Code settings (usually personal and should not go into the repo)
.vscode/
# JetBrains/IntelliJ settings
.idea/

# --- Dependencies (NodeJS) ---
# If you install npm packages in the future (to update the theme, for example)
node_modules/
package-lock.json
yarn.lock

# --- Security  ---
# Never upload files with API keys or passwords
.env

